{
	"name": "cora-pic-g",
	"version": "1",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --env prod --minify src/index.ts",
		"dev": "wrangler dev --env dev --remote src/index.ts"
	},
	"dependencies": {
		"hono": "^4.2.2"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240403.0",
		"wrangler": "^3.47.0",
		"@types/node": "^20.12.5"
	}
}
